apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: percy-blog-ingress
spec:
  rules:
    - http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: percy-blog-service
              port:
                number: 80

